基于mapdb内嵌数据库封装
	支持多个文件操作，合服处理直接复制文件即可
*测试结果 mapdb 开了mmap技术5W写入两秒左右，数据量越大写入越慢，不开启mmap 每秒写入2.5K
*mapdb有个很严重的BUG，开多个数据库或开多个线程压测发现死锁，看了底层锁代码写得好烂，这个BUG导致无法使用
*其它两个数据库 Xodus，ChronicleMap 号称好快，看了底层是偷偷开启mmap技术，一压直接崩了，而且没MAPDB提供存储编码友好，还有与JAVA8 MAP API 结合高度抽象
